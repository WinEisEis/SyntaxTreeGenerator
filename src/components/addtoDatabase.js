import React from 'react';
import { Button } from 'reactstrap';

const AddtoDatabase = (props) => {
    return(
        <div>
        <Button color="primary" size="lg" active >Add to database</Button>
        </div>
    );
};

export default AddtoDatabase;